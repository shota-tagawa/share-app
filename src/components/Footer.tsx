import React from 'react';
import { BottomNavbar } from './'

const Footer = () => {
  return (
    <footer style={{ paddingBottom: 64 }}>
      <BottomNavbar />
    </footer>
  )
}

export default Footer;